function indices = generate_keyed_permutation(size)
result = []    

for i = 1:size
    % generage new index
    newInd = 5*i + 7;
    % if found index is already in result
    while ismember(newInd, result)
        newInd = mod(newInd + 1, size)
    end
end
end